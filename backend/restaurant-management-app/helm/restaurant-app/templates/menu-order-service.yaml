{{- if .Values.menuOrderService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: menu-order-service
  namespace: {{ .Values.namespace.name }}
  labels:
    app: menu-order-service
spec:
  replicas: {{ .Values.menuOrderService.replicas }}
  selector:
    matchLabels:
      app: menu-order-service
  template:
    metadata:
      labels:
        app: menu-order-service
    spec:
      containers:
      - name: menu-order-service
        image: "{{ .Values.menuOrderService.image.repository }}:{{ .Values.menuOrderService.image.tag }}"
        imagePullPolicy: {{ .Values.menuOrderService.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.menuOrderService.port }}
          name: http
        env:
        - name: KAFKA_BROKER
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: KAFKA_BROKER
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: DB_HOST
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: DB_PORT
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongodb-secret
              key: password
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: menu-order-service-config
              key: DB_NAME
        - name: MONGODB_URI
          valueFrom:
            configMapKeyRef:
              name: menu-order-service-config
              key: MONGODB_URI
        resources:
          {{- toYaml .Values.menuOrderService.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.menuOrderService.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.menuOrderService.port }}
          initialDelaySeconds: 10
          periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: menu-order-service
  namespace: {{ .Values.namespace.name }}
  labels:
    app: menu-order-service
spec:
  type: {{ .Values.menuOrderService.service.type }}
  ports:
  - port: {{ .Values.menuOrderService.port }}
    targetPort: {{ .Values.menuOrderService.port }}
    protocol: TCP
    name: http
    {{- if eq .Values.menuOrderService.service.type "NodePort" }}
    nodePort: {{ .Values.menuOrderService.service.nodePort }}
    {{- end }}
  selector:
    app: menu-order-service
{{- end }}



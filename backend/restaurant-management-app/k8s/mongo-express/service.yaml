apiVersion: v1
kind: Service
metadata:
  name: mongo-express
  namespace: restaurant-app
  labels:
    app: mongo-express
spec:
  type: NodePort
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
    nodePort: 30081
  selector:
    app: mongo-express


